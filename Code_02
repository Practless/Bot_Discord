const lib = require('lib')({token: process.env.STDLIB_SECRET_TOKEN});
let result = await lib.discord.users['@0.1.1'].me.status.update({
  activity_name: `K.Flay - High Enough`, // The custom status!
  activity_type: 'LISTENING', // GAME, STREAMING, LISTENING, WATCHING, COMPETING
  status: 'ONLINE', // Change this to DND or IDLE.
});
